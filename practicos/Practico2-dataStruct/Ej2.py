# Lista simulada con 1 millón de usuarios
usuarios = [f"usuario_{i}" for i in range(1000000)]

# Accede directamente al usuario en la posición 500,000
usuario_deseado = usuarios[499999]

print(f"El usuario en la posición 500,000 es: {usuario_deseado}")

